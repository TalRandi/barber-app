{
  "installCommand": "npm install --include=dev && cd frontend && npm install --include=dev && cd ../backend && npm install --include=dev && npx prisma generate",
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
